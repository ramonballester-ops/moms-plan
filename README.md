# Mom's Plan

A private family document, delivered as a small installable web app.

The page content is encrypted (AES-256-GCM, key derived from a passcode via
PBKDF2). This repository hosts only the generic app shell, a static charting
library, and the encrypted blob — no readable personal information.
